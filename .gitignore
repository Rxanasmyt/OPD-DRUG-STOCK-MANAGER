node_modules
dist
.DS_Store
*.local
tsconfig.tsbuildinfo

# Local output of scripts/backup-firestore.mjs — never committed (see .github/workflows/firestore-backup.yml, which uploads it as an Actions artifact instead)
backups/
